name: hello
on:
  workflow_dispatch:
  pull_request: {}
jobs:
  hello:
    name: run hello
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: print
        run: |
          echo in test-action3/action.yml  in main
          echo pwd
          echo "$pwd"
          echo $pwd
          
